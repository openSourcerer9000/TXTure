package:
  name: "txture"
  version: "0.3.0"

source:
  # Relative path to the parent directory.
  path: ..

requirements:
  host:
    - python
    - setuptools
  build:
    - python {{ python }}
  run:
    - python
    - funkshuns

about:
  home: https://github.com/openSourcerer9000/TXTure
  license: MIT
  license_familY: MIT
  license_file: LICENSE
  summary: "Surgically edit machine-readable ASCII files"

extra:
  recipe-maintainers:
    - openSourcerer9000